name: fk_army_builder
description: Warhammer 40k Army Builder
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.13.0'

dependencies:
  flutter:
    sdk: flutter

   # Drift
  drift: ^2.14.0
  sqlite3_flutter_libs: ^0.5.20
  sqlite3: ^2.4.0

  http: ^1.1.0
  
  provider: ^6.1.1

  path_provider: ^2.1.1
  path: ^1.9.0
  shared_preferences: ^2.2.2

  csv: ^6.0.0

  flutter_markdown: ^0.6.18
  
dev_dependencies:
  flutter_test:
    sdk: flutter

  # Drift code generator
  drift_dev: ^2.14.0

  build_runner: ^2.4.6
  build_resolvers: ^2.4.1

  flutter_lints: ^3.0.1

dependency_overrides:
  # Фиксируем версии для совместимости
  build_runner: ^2.4.6
  drift_dev: ^2.28.0
  #_fe_analyzer_shared: ^67.0.0
  # analyzer: ^10.0.2
 
flutter:
  uses-material-design: true